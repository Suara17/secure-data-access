# Secure Data Access 项目启动指南

## 项目概述

这是一个基于 React + TypeScript + Vite 的安全数据访问应用，提供了用户认证、角色管理和数据访问控制功能。

### 主要功能

- 🔐 用户认证系统
- 👨‍💼 管理员仪表板
- 👤 用户工作空间
- 🛡️ 基于角色的访问控制
- 🎨 现代化 UI (shadcn/ui + Tailwind CSS)

## 技术栈

- **前端框架**: React 18
- **开发工具**: Vite
- **编程语言**: TypeScript
- **UI 组件库**: shadcn/ui + Radix UI
- **样式**: Tailwind CSS
- **路由**: React Router DOM
- **状态管理**: React Query + Context API
- **表单处理**: React Hook Form + Zod

## 系统要求

- Node.js 16+
- npm 或 yarn 或 bun
- Git

## 安装步骤

### 1. 克隆项目

```bash
git clone <repository-url>
cd secure-data-access-main
```

### 2. 安装依赖

#### 使用 npm（推荐）
```bash
npm install
```

#### 使用 yarn
```bash
yarn install
```

#### 使用 bun（如果可用）
```bash
bun install
```

### 3. 启动开发服务器

```bash
npm run dev
```

服务器将在 `http://localhost:8080` 启动。

## 项目结构

```
src/
├── components/          # 可复用组件
│   ├── ui/             # shadcn/ui 组件
│   ├── AccessDeniedModal.tsx
│   ├── NavLink.tsx
│   └── SecurityLevelBadge.tsx
├── contexts/           # React Context
│   └── AuthContext.tsx
├── data/               # 模拟数据
│   └── mockData.ts
├── hooks/              # 自定义 Hooks
├── lib/                # 工具函数
│   └── utils.ts
├── pages/              # 页面组件
│   ├── AdminDashboard.tsx
│   ├── Login.tsx
│   ├── NotFound.tsx
│   └── UserWorkspace.tsx
├── types/              # TypeScript 类型定义
│   └── security.ts
└── main.tsx           # 应用入口
```

## 可用脚本

在项目目录中运行：

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 构建开发版本
npm run build:dev

# 预览构建结果
npm run preview

# 代码检查
npm run lint
```

## 应用路由

| 路径 | 说明 | 权限要求 |
|------|------|----------|
| `/login` | 登录页面 | 无需登录 |
| `/admin` | 管理员仪表板 | 管理员 |
| `/workspace` | 用户工作空间 | 已登录用户 |
| `/` | 重定向到登录页 | 无 |
| `*` | 404 页面 | 无 |

## 开发指南

### 环境变量

项目支持以下环境变量（可选）：

```env
# .env 文件
VITE_API_BASE_URL=http://localhost:3001/api
```

### 代码规范

- 使用 TypeScript 进行类型检查
- 遵循 ESLint 配置
- 使用 Prettier 格式化代码（通过编辑器插件）

### 添加新组件

1. 在 `src/components/` 中创建组件
2. 如果是 UI 组件，考虑使用 shadcn/ui
3. 导出组件并在需要的地方导入

### 添加新页面

1. 在 `src/pages/` 中创建页面组件
2. 在 `src/App.tsx` 中添加路由
3. 根据需要添加权限控制

## 部署

### 构建生产版本

```bash
npm run build
```

构建文件将在 `dist/` 目录生成。

### 部署选项

1. **静态托管**: 将 `dist/` 目录部署到 Netlify、Vercel 等平台
2. **服务器部署**: 需要配置后端 API 服务器
3. **Docker**: 可以添加 Dockerfile 进行容器化部署

## 故障排除

### 常见问题

1. **端口占用**: 如果 8080 端口被占用，可以修改 `vite.config.ts` 中的端口配置
2. **依赖安装失败**: 尝试删除 `node_modules` 和锁文件，然后重新安装
3. **构建失败**: 检查 TypeScript 错误和 ESLint 警告

### 获取帮助

- 查看项目 README.md
- 检查 package.json 中的依赖版本
- 查看浏览器开发者工具的控制台输出

## 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 发起 Pull Request

---

**最后更新**: 2024年12月25日
